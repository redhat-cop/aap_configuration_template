---
all:
  children:
    prod:
      hosts:
        127.0.0.1: # Due to the way this template is configured, DO NOT CHANGE THIS!
      vars:
        connection: local
    automationcontroller:
      hosts:
        HERE: # FQDN of Automation Controller host 

    automationhub:
      hosts:
        HERE: # FQDN of Automation Hub host. Can be commented out if not in use.

    builder:
      hosts:
        HERE: # FQDN of builder server if you have one. Otherwise, can be Automation Hub FQDN or commented out if not in use.

    # only needed if installing AAP with automation, can be removed if you are not. See Install docs if unsure what server should be the database host.
    database:
      hosts:
        HERE:
  vars:
    env: prod
...
