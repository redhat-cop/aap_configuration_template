---
all:
  children:
    dev:
      hosts:
        127.0.0.1:
      vars:
        connection: local
    automationcontroller:
      hosts:
        HERE:

    automationhub:
      hosts:
        HERE:

    builder:
      hosts:
        HERE:

    database:
      hosts:
        HERE:
  vars:
    env: dev
...
