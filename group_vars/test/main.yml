---
controller_applications: "{{ controller_applications_all + controller_applications_test }}"
controller_credential_input_sources: "{{ controller_credential_input_sources_all + controller_credential_input_sources_test }}"
controller_credential_types: "{{ controller_credential_types_all + controller_credential_types_test }}"
controller_credentials: "{{ controller_credentials_all + controller_credentials_test }}"
controller_execution_environments: "{{ controller_execution_environments_all + controller_execution_environments_test }}"
controller_groups: "{{ controller_groups_all + controller_groups_test }}"
controller_hosts: "{{ controller_hosts_all + controller_hosts_test }}"
controller_instance_groups: "{{ controller_instance_groups_all + controller_instance_groups_test }}"
controller_inventories: "{{ controller_inventories_all + controller_inventories_test }}"
controller_inventory_sources: "{{ controller_inventory_sources_all + controller_inventory_sources_test }}"
controller_templates: "{{ controller_templates_all + controller_templates_test }}"
controller_labels: "{{ controller_labels_all + controller_labels_test }}"
controller_notifications: "{{ controller_notifications_all + controller_notifications_test }}"
controller_organizations: "{{ controller_organizations_all + controller_organizations_test }}"
controller_projects: "{{ controller_projects_all + controller_projects_test }}"
controller_roles: "{{ controller_roles_all + controller_roles_test }}"
controller_schedules: "{{ controller_schedules_all + controller_schedules_test }}"
controller_settings: "{{ controller_settings_all | combine(controller_settings_test, list_merge='append', recursive=true) }}"
controller_teams: "{{ controller_teams_all + controller_teams_test }}"
controller_user_accounts: "{{ controller_user_accounts_all + controller_user_accounts_test }}"
controller_workflows: "{{ controller_workflows_all + controller_workflows_test }}"
env: test
scm_branch: "" # when you want it to be default branch just set to ""
...
